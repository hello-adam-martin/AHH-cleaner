# Backend API Configuration (for Expo client)
# The API URL - defaults to /api for same-domain deployment on Vercel
# For local development with vercel dev, /api also works
# EXPO_PUBLIC_API_URL=/api

# ===========================================
# Server-side only (Vercel environment variables)
# These should be set in Vercel dashboard, NOT exposed to client
# ===========================================

# Your Airtable API key (Personal Access Token)
# Get it from: https://airtable.com/create/tokens
AIRTABLE_API_KEY=your_api_key_here

# Your Airtable Base ID
# Find it in your base URL: https://airtable.com/{BASE_ID}/...
AIRTABLE_BASE_ID=your_base_id_here

# Airtable Table Names
AIRTABLE_BOOKINGS_TABLE=Bookings
AIRTABLE_PROPERTIES_TABLE=Properties
AIRTABLE_CLEANERS_TABLE=Cleaners

# Field Names in your Bookings table
AIRTABLE_CHECKOUT_DATE_FIELD=Check Out
AIRTABLE_PROPERTY_LINK_FIELD=Property
AIRTABLE_CHECKIN_DATE_FIELD=Check In
AIRTABLE_CLEANING_DURATION_FIELD=Cleaning Time
AIRTABLE_CONSUMABLES_COST_FIELD=Linen Costs
